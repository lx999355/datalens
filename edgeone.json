{
  "installCommand": "npm install --omit=dev && npm install @tailwindcss/postcss tailwindcss",
  "buildCommand": "npx prisma generate && npm run build && node scripts/cleanup-standalone.js",
  "outputDirectory": ".next/standalone",
  "nodeVersion": "20.18.0",
  "cloudFunctions": {
    "nodejs": {
      "maxDuration": 30
    }
  },
  "headers": [
    {
      "source": "/api/*",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        }
      ]
    }
  ]
}
